import {AddExpenseTemplate} from '../components/AddExpenseFrame';

export default function AddPowerExpense({ navigation }) {
  return(
    <AddExpenseTemplate category = "Power"></AddExpenseTemplate>
  );
}